---
- hosts: localhost
  gather_facts: yes
  tasks:
    - name: Verify JSON format environment variables are accessible
      assert:
        that:
          - ansible_env.TEST_JSON_VAR is defined
          - ansible_env.TEST_JSON_VAR == "json_test_value"
        fail_msg: "JSON format environment variables not accessible via -E option"
